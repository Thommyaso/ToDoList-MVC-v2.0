export default {
    log(message) {
        // eslint-disable-next-line no-console
        console.log(message);
    },
    error(error) {
        // eslint-disable-next-line no-console
        console.error(error);
    },
};
